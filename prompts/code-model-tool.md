Recommend an execution path (`recommend`), switch the SAME conversation to its configured coding model and effort (`start`), restore the original model and effort for review (`finish`), or inspect settings (`status`). Automatic routing may be configured as `off`, `observe`, or `enforce`; when it is active, a `before_agent_start` gate asks TypeSafe Jev before the user prompt reaches the provider and records the result. With automatic routing off, call `recommend` with a concise task summary before choosing the executor. Set `allowSubagent` only when the user has explicitly authorised subagent use. A TypeSafe credential sends the task summary and sanitised model and quota facts to Jev; the configured fallback handles credential or request failures. Recommendations record their backend, confidence, quota state and fallback reason. When a recommendation selects a coding phase, call `start` as a standalone tool call and pass the recommended `effort`; omitting `effort` uses the saved profile. The coding model edits and runs targeted checks directly in the same conversation, then calls `finish` alone for original-model review. Existing permissions and provider safety approvals continue to apply.
